{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "inputs": [
    {
      "id": "buildConfig",
      "type": "pickString",
      "description": "Select a build configuration",
      "options": [
        "Release",
        "Debug"
      ],
      "default": "Release"
    },
    {
      "id": "runtimeId",
      "type": "pickString",
      "description": "Select a runtime identifier",
      "options": [
        "linux-x64",
        "win-x64"
      ],
      "default": "win-x64"
    },
  ],
  "tasks": [
    {
      "label": "$(build) Build Project: LuaLoader",
      "command": "dotnet",
      "type": "shell",
      "args": [
        "build",
        "-c",
        "${input:buildConfig}",
      ],
      "group": "build",
      "presentation": {
        // "reveal": "silent"
      },
      "problemMatcher": "$msCompile",
      "options": {
        "cwd": "${workspaceFolder}/LuaLoader"
      }
    },
    {
      "label": "$(tools) Publish Project: LuaLoader",
      "command": "dotnet",
      "type": "shell",
      "args": [
        "publish",
        "-c",
        "${input:buildConfig}",
        "-r",
        "${input:runtimeId}",
      ],
      "group": "build",
      "presentation": {
        // "reveal": "silent"
      },
      "problemMatcher": "$msCompile",
      "options": {
        "cwd": "${workspaceFolder}/LuaLoader"
      }
    }
  ]
}
